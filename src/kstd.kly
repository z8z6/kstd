module kstd;

// Library entry point: it imports every public standard module so that one
// linked object carries the whole library. Programs build against the library
// and link this object instead of recompiling the modules.
import std.ascii;
import std.io;
import std.math.basic;
import std.math.integer;
