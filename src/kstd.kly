module kstd;

// Library entry point: import modules that can be linked without a host CRT.
// Other modules remain available from source for host programs.
import std.alloc;
import std.annotation;
import std.ascii;
import std.file;
import std.io;
import std.math.integer;
import std.option;
import std.reflect;
import std.signal;
import std.string;
import std.thread;
